// *yytext is any time you want to return something


//flex ex1.flex
// gcc lex.yy.c -lfl -o scanner
./scanner

// For part 1, print in main not in rules

"//".*	ECHO; 
will not work

Need 
"//".*	ECHO;
"//".*\n	ECHO;	// New problem. This one also returns the \n character

Comments can be only used in initial declarations and in c code in main.

"\""
\\\"

"\\\n"
to catch any escape character?

Create 2 new states that it goes to and then only looks for else if
Add preprocessor directives support?

Review how to implement state machines in FLEX